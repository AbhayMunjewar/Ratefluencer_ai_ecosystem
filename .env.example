# YouTube Data API v3 (required for Campaign Success YouTube search + channel sync)
# Create key: Google Cloud Console → APIs & Services → enable "YouTube Data API v3"
YOUTUBE_API_KEY=your_key_here

# Optional: frontend API URL (dev uses Vite proxy /api → :8000 when unset)
# VITE_API_BASE_URL=http://localhost:8000
